import React from 'react';
import Navbar from "../components/Navbar";
const Profile = () => {
    return (
        <div>
            <Navbar/>
            Your profile
        </div>
    );
};

export default Profile;